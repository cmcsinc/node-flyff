{
  "name": "@flyff/mail",
  "version": "1.0.0",
  "description": "Flyff mail (post) domain -- admin->player mail: mailbox snapshot, read/take-item/take-gold/delete handlers, MODE_MAILBOX indicator",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src",
    "test": "node ../../scripts/run-tests.mjs test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@flyff/core": "workspace:*",
    "@flyff/entities": "workspace:*",
    "@flyff/world-core": "workspace:*",
    "@flyff/database": "workspace:*",
    "@flyff/inventory": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  }
}
